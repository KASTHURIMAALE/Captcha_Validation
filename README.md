# Captcha_Validation
A simple CAPTCHA validation system built using HTML, CSS, and JavaScript. It generates a random CAPTCHA with a noisy canvas background, allowing users to verify input. Includes refresh functionality and validation with success or error alerts.
